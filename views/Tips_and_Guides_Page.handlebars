
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tips and Guides for Sustainability</title>
    <style>
        body { font-family: Arial, sans-serif; line-height: 1.6; margin: 0; padding: 0; color: #333; background-color: #f4f4f4; }
        .container { max-width: 1200px; margin: auto; padding: 20px; }
        header, footer { background: #333; color: #fff; padding: 10px 0; text-align: center; }
        h1, h2 { color: #008000; }
        p { margin: 20px 0; }
        .button { display: inline-block; padding: 10px 20px; margin: 10px 0; background: #008000; color: white; text-decoration: none; border-radius: 5px; }
        input[type="search"] { padding: 10px; width: 50%; margin-top: 20px; }
        ul { list-style-type: none; padding: 0; }
        li { margin: 10px 0; padding: 10px; background: white; border: 1px solid #ddd; }
        .category { font-weight: bold; }
    </style>
</head>
<body>
    <header>
        <div class="container">
            <h1>Sustainability Tips and Guides</h1>
        </div>
    </header>
    <div class="container">
        <input type="search" placeholder="Search tips...">
        <ul>
            {{#each categories}}
            <li class="category">{{this.categoryName}}</li>
            {{#each this.tips}}
            <li>
                <h2>{{this.title}}</h2>
                <p>{{this.content}}</p>
            </li>
            {{/each}}
            {{/each}}
        </ul>
    </div>
    <footer>
        <div class="container">
            <p>Contact Us: info@klimate.com | Follow us on social media</p>
        </div>
    </footer>
</body>
</html>
